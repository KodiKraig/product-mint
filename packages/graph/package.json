{
  "name": "@product-mint/graph",
  "version": "0.0.1",
  "author": "@kodikraig",
  "description": "GraphProtocol for ProductMint contracts with GraphQL schemas.",
  "license": "BUSL-1.1",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ productmint-base",
    "create-local": "graph create --node http://localhost:8020/ productmint-base",
    "remove-local": "graph remove --node http://localhost:8020/ productmint-base",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 productmint-base",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.0",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
