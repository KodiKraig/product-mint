export const logEventHeading = (eventName: string, totalEvents: number) => {
  console.log('--------------------------------');
  console.log(`${eventName} Events`);
  console.log('--------------------------------');
  console.log(`Total events: ${totalEvents}`);
};
